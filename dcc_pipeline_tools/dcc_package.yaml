#Anchorpoint Markup Language
#Predefined Variables: e.g. ${path}
#Environment Variables: e.g. ${MY_VARIABLE}
#Full documentation: https://docs.anchorpoint.app/docs/actions/create-actions

version: "1.0"

action:
  #Must Have Properties
  name: DCC Pipeline Tools

  #Optional Properties
  version: 1
  id:  ap::package::dcc
  category: vc
  type: package
  enable: false
  description: Publish file versions based on <a href='https://docs.anchorpoint.app/docs/asset-management/incremental-file-versioning/'>incremental file versioning</a> for digital content creation tools. Adds a new project type for shared folders and DCC integrations.
  icon:
    path: dccs.svg

  settings: "dcc_action_settings.py"
  author: "Anchorpoint Software GmbH"

  actions:
    -  ap::inc::timeline
    -  ap::inc::project
    -  ap::inc::project-settings
    -  ap::inc::publish
    -  ap::integrations::cinema-4d

    