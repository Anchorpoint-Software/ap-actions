# Anchorpoint Markup Language
# Predefined Variables: e.g. ${path}
# Environment Variables: e.g. ${MY_VARIABLE}
# Full documentation: https://docs.anchorpoint.app/Actions/Reference

version: 1.0
action:
  name: On Is Action Enabled Trigger

  version: 1
  id: ap::examples::trigger::actionenabled
  type: python
  author: Anchorpoint Software GmbH
  description: "Example that invokes a script to check if an action is enabled or not"
  icon:
    path: :/icons/action.svg

  script: on_is_action_enabled_example.py
  register:
    folder:
      enable: on_is_action_enabled_example.py

