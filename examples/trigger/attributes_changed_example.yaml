# Anchorpoint Markup Language
# Predefined Variables: e.g. ${path}
# Environment Variables: e.g. ${MY_VARIABLE}
# Full documentation: https://docs.anchorpoint.app/Actions/Reference

version: 1.0
action:
  name: On Attributes Changed

  version: 1
  id: ap::examples::trigger::attributeschanged
  type: python
  author: Anchorpoint Software GmbH
  description: "An example trigger that is executed whenever attributes are changed"
  icon:
    path: :/icons/action.svg

  script: attributes_changed_example.py

  register:
    trigger:
      on_attributes_changed:
        enable: true
