# Anchorpoint Markup Languageintegrations
# Predefined Variables: e.g. ${path}
# Environment Variables: e.g. ${MY_VARIABLE}
# Full documentation: https://docs.anchorpoint.app/docs/actions/create-actions

version: 1.0
action:
  name: Gitlab Integration

  version: 1
  id: ap::integrations::gitlab
  category: integrations
  type: python
  enable: true
  author: Anchorpoint Software GmbH
  description: Integration for Gitlab

  script: gitlab/gitlab_integration.py
