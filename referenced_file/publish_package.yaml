#Anchorpoint Markup Language
#Predefined Variables: e.g. ${path}
#Environment Variables: e.g. ${MY_VARIABLE}
#Full documentation: https://docs.anchorpoint.app/api/intro

version: "1.0"

action:
  #Must Have Properties
  name: "Create Referenced File"

  #Optional Properties
  version: 1
  id: ap::package::referenced::file
  category: user
  type: package
  enable: false
  description: Creates <a href='https://docs.anchorpoint.app/assets/browser/#creating-referenced-files'>(publishes)</a> a copy of the latest version in the incremental stack and removes the increment. Useful for importing an asset into a shot or assembling scenes.

  author: "Anchorpoint Software GmbH"
  icon:
    path: "extract.svg"

  actions:
    - ap::referenced::file


    