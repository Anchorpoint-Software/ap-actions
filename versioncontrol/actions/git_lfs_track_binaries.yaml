# Anchorpoint Markup Language
# Predefined Variables: e.g. ${path}
# Environment Variables: e.g. ${MY_VARIABLE}
# Full documentation: https://docs.anchorpoint.app/Actions/Reference

version: 1.0
action:
  name: Git LFS Track Binaries

  version: 1
  id: git::lfstrackall
  category: git
  type: python
  author: Anchorpoint Software GmbH
  description: Checks the repository for binary files and tracks them with LFS
  icon:
    path: :/icons/version.svg

  script: git_lfs_track_binaries.py

  register:
    folder:
      enable: true